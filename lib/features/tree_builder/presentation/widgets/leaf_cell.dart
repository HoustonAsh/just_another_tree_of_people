import 'package:flutter/material.dart';

class LeafCell extends StatelessWidget {
  const LeafCell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
