import 'package:flutter/material.dart';

class NodeCell extends StatelessWidget {
  const NodeCell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
